use bevy::prelude::*;

#[derive(Component)]
struct WireParticle;

struct MyPlugin;

impl Plugin for MyPlugiiiiiiin
{
    fn build(&self, app: &mut App)
    {
        let count = 15;
        for i in 1..count
        {
        
    }
}

fn build_wave(mut commands: Commands)
{
    let count = 15;
    for i in 1..count
    {
        commands.spawn((SpriteBundle{ sprite: Sprite::new(Vec2::new(( }, WireParticle));
    }
}

fn main() {
    App::new()
        .add_plugins(DefaultPlugins)
        .add_plugin()
    println!("Hello, world!");
}
